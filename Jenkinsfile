pipeline {
  agent any  // run on any available Jenkins agent
  stages {
    stage('Build') {
      steps {
        echo "Building on ${env.BRANCH_NAME}"  // prints the branch name
      }
    }
  }
}
